import { url } from '../../constants'
import Status from './status'

const ind = {
    routeProps: {
        path: url.STATUSES,
        element: <Status />,
    },
    name: 'Statuses ',
}

export default ind