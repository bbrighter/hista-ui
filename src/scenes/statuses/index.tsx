import { url } from '../../constants'
import Status from './temp'

const ind = {
    routeProps: {
        path: url.STATUSES,
        element: <Status />,
    },
    name: 'Statuses ',
}

export default ind