import { url } from '../../constants'
import Statistics from './Statistics'

const ind = {
    routeProps: {
        path: url.STATISTICS,
        element: <Statistics />,
    },
    name: 'Statistics',
}

export default ind