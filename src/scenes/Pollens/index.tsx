import { url } from "../../constants"
import Pollensview from "./Pollens"


const ind = {
    routeProps: {
        path: url.POLLENS,
        element: <Pollensview />,
    },
    name: "Pollens"
}

export default ind