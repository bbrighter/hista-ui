import { url } from "../../constants"
import Pollens from "./Pollens"


const ind = {
    routeProps: {
        path: url.POLLENS,
        element: <Pollens />,
    },
    name: "Pollens"
}

export default ind